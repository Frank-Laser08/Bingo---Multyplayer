git clone https://github.com/Frank-Laser08/Bingo---Multyplayer.git
cd Bingo---Multyplayer

# Tambahkan isi folder rapi (client + server + README)
git add .
git commit -m "Struktur proyek rapi: frontend & backend dipisah"
git push
